# CHANGELOG
## v0.0.2
* added callbacks parameter `label`
* added callback handling for:
  * `activeIcon`
  * `inactiveIcon`
  * `tileActiveClass`
  * `tileInactiveClass`
## v0.0.1
* changelog created
* initial version, lets call it 'beta'